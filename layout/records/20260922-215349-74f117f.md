# DRC/LVS run `20260922-215349-74f117f` -- `testcell`

Produced by `python3 layout/drclvs.py --cell testcell --record`. Append-only:
a re-run mints a new record rather than editing this one.

| | |
| --- | --- |
| Cell | `drclvs_testcell` (layout top cell `DRCLVS_TESTCELL`) -- one nfet_03v3, W=2 um, L=0.28 um, nf=1 |
| PDK | gf180mcu variant `gf180mcuD`, open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b` |
| klayout | `KLayout 0.30.10` |
| klt | `klt 0.4.0+g5c757081680e.dirty` |
| xschem | `XSCHEM V3.4.7` |
| pmap | `shimmed` (the PDK decks' memory logger; `shimmed` = this host has no procps pmap(1) and `drclvs.py` supplied a ps(1)-backed stand-in) |

## Results

| Stage | Result |
| --- | --- |
| DRC -- `klt drc --deck gf180mcu` | **clean**, 0 violation(s); layout dbu 0.001 um |
| DRC -- PDK deck (41 rule tables) | **0 violation(s)** across 640 rule categories |
| LVS -- PDK `gf180mcu.lvs` vs. xschem export | **match** |
| LVS negative control, topology (gate shorted to drain) | **mismatch, as required** |
| LVS negative control, parameter (device width doubled) | **mismatch, as required** |

Schematic netlist: 1 device line(s), exported in
xschem's `lvs_netlist` form from `layout/testcell/drclvs_testcell.sch`.

## What this does and does not establish

- The flow runs end to end: PDK PCell -> GDS -> DRC (two decks) -> LVS against
  a netlist exported from the schematic source, with negative controls proving
  the compare is live in both topology and device parameters.
- It says **nothing** about the LDO. The cell is one transistor; no block-level layout exists yet.
- `klt drc`'s deck is a curated subset (see `layout/README.md`, "Coverage,
  honestly"); the PDK deck's 640 categories are the number
  worth quoting.

# DRC/LVS run `20260923-183338-f641909` -- `passives`

Produced by `python3 layout/drclvs.py --cell passives --record`. Append-only:
a re-run mints a new record rather than editing this one.

| | |
| --- | --- |
| Cell | `drclvs_passives` (layout top cell `DRCLVS_PASSIVES`) -- one ppolyf_u_1k H-poly resistor, one cap_mim_2f0 MIM cap, and one 2-finger nfet_03v3 -- the passive-bearing flow vehicle (issue #313) |
| PDK | gf180mcu variant `gf180mcuD`, open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b` |
| klayout | `KLayout 0.28.16` |
| klt | `klt 0.5.0` |
| xschem | `XSCHEM V3.4.7` |
| pmap | `host` (the PDK decks' memory logger; `shimmed` = this host has no procps pmap(1) and `drclvs.py` supplied a ps(1)-backed stand-in) |

## Results

| Stage | Result |
| --- | --- |
| DRC -- `klt drc --deck gf180mcu` | **clean**, 0 violation(s); layout dbu 0.001 um |
| DRC -- PDK deck (41 rule tables) | **0 violation(s)** across 642 rule categories |
| LVS -- PDK `gf180mcu.lvs` vs. xschem export | **match** |
| LVS negative control, topology (gate shorted to drain) | **mismatch, as required** |
| LVS negative control, parameter (device width doubled) | **mismatch, as required** |
| LVS negative control, passive-r (resistor length doubled) | **mismatch, as required** |
| LVS negative control, passive-c (capacitor length doubled) | **mismatch, as required** |

Schematic netlist: 3 device line(s), exported in
xschem's `lvs_netlist` form from `layout/passives/drclvs_passives.sch`.

## What this does and does not establish

- The flow runs end to end: PDK PCell -> GDS -> DRC (two decks) -> LVS against
  a netlist exported from the schematic source, with 4
  negative control(s) (parameter, passive-c, passive-r, topology) proving the compare is live in each of
  those respects rather than silently comparing nothing.
- It says **nothing** about the LDO. The cell is three unconnected devices that exist only to prove the flow handles a netlist whose devices are not all FETs; no block-level layout exists yet.
- `klt drc`'s deck is a curated subset (see `layout/README.md`, "Coverage,
  honestly"); the PDK deck's 642 categories are the number
  worth quoting.

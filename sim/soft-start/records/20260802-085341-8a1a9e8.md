# Record 20260802-085341-8a1a9e8

**Provenance erratum.** This record corrects one field of
`20260802-023437-5ea33e8` — its `git:` line — and nothing else. No
simulation was run for it and no measured value is changed, added or
restated: for the results, read `20260802-023437-5ea33e8`, which stands
exactly as written.

- **Record ID**: 20260802-085341-8a1a9e8
- **Supersedes**: `20260802-023437-5ea33e8` (provenance only — see above)
- **Claim**: the provenance of `20260802-023437-5ea33e8` is resolvable in a
  fresh clone. The claim that record itself carries (the `README.md`
  "Startup" target-spec row) is untouched here.
- **The defect**: `20260802-023437-5ea33e8`'s Environment block reads

  ```
  - git: `5ea33e8` on `feature/issue-55`
  ```

  `5ea33e8` was rebased/amended away during review of PR #62 and is on **no
  branch**. It is reachable today only from a local reflog, so a fresh clone
  of this repository cannot resolve it and the record's provenance is, as
  published, a dead pointer.

- **The correction**: the commit that carries that tree on `feature/issue-55`
  is **`07358b7`**, an ancestor of the branch head. The corrected line reads:

  ```
  - git: `07358b7` on `feature/issue-55`
    (recorded at the time as `5ea33e8`, a commit later rebased away;
     `git diff 5ea33e8 07358b7 -- design` is documentation-only)
  ```

- **Evidence that the measurement is unaffected** — two independent checks,
  both re-runnable from committed files:

  1. `git diff 5ea33e8 07358b7 -- design` touches `design/error_amp.md`
     (+94 lines) and `design/error_amp.sch` (+19 lines of schematic *note*
     text) only. `git diff 5ea33e8 07358b7 -- design/netlist` is **empty**:
     all four generated netlists are byte-identical across the two commits.
  2. Stronger, and independent of `5ea33e8` existing at all: the DUT netlist
     this record froze is committed next to it, and it is byte-identical to
     the design netlist at the branch head —

     ```
     $ shasum -a 256 sim/soft-start/netlist-snapshots/20260802-023437-5ea33e8.spice \
                     design/netlist/ldo_core.spice
     15f70738b1a1b9234770a3b92034b3c7bb85b2904e86ec0ccd53237377470f1a  sim/soft-start/netlist-snapshots/20260802-023437-5ea33e8.spice
     15f70738b1a1b9234770a3b92034b3c7bb85b2904e86ec0ccd53237377470f1a  design/netlist/ldo_core.spice
     ```

     `design/netlist/ldo_core.spice` is the fully expanded hierarchy
     (`error_amp`, `ldo_ilimit`, `ldo_softstart` and the pass device are all
     inside it), so this single hash covers the whole DUT. Whatever commit
     the run was launched from, the circuit it measured is the circuit at
     `8a1a9e8`.

- **Second correction, to PR #62's Test Plan rather than to the record**: the
  PR body claimed every cited record's provenance line reads `(clean)`.
  `20260802-023437-5ea33e8` follows the older hand-written Environment-block
  format and carries **no** cleanliness marker at all — neither `(clean)` nor
  `(dirty)`. The claim was false for this record and has been corrected in
  the PR body; the working tree's state at run time is not recoverable from
  the record and is not asserted here.

- **Netlist provenance**: unchanged — schematic, via
  `sim/soft-start/netlist-snapshots/20260802-023437-5ea33e8.spice`.
- **Corner matrix run / Operating conditions / Statistical convention /
  Result**: unchanged and deliberately not restated. See
  `20260802-023437-5ea33e8`; duplicating its tables here would create a
  second copy of the evidence that could drift from the first.
- **Links**:
  - Corrected record: `sim/soft-start/records/20260802-023437-5ea33e8.md`
  - Its raw logs: `sim/soft-start/corners/20260802-023437-5ea33e8/`
  - Its netlist snapshot:
    `sim/soft-start/netlist-snapshots/20260802-023437-5ea33e8.spice`
  - Sibling errata for the same defect:
    `sim/enable-shutdown/records/20260802-085340-8a1a9e8.md`,
    `sim/current-limit/records/20260802-085342-8a1a9e8.md`
- **Timestamp / author**: 2026-08-02T08:53:41Z, Claude Opus 5
  <noreply@anthropic.com> (issue #55, PR #62 review round 2)

## Environment

- git: `8a1a9e8` on `feature/issue-55` (clean at generation time)
- No PDK, no simulator, no command: this record is a documentation
  correction derived from committed files (`git diff`, `shasum`).

---

Append-only: never edit or delete this file — a re-run or correction mints a
new record-id and points back here via **Supersedes**.

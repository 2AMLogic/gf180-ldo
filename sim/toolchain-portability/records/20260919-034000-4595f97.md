# Record 20260919-034000-4595f97

- **Record ID**: 20260919-034000-4595f97
- **Claim**: issue #254's fact (b) / DR-0025's **F2** -- `wnflag=0` (and no
  `.options` card at all) fails to parse `design/netlist/ldo_core.spice`'s
  pass-device geometry with `could not find a valid modelname`, never a
  silent clamp to a neighbouring bin, and a genuinely out-of-range
  per-finger width hard-errors even at `wnflag=1` -- reproduces on ngspice
  majors other than the 46 DR-0025 established it on.
- **Netlist provenance**: N/A -- toolchain-portability measurement (see
  `sim/toolchain-portability/README.md`), not a DUT-netlist simulation. Same
  inline pass-device geometry as record `20260919-033900-4595f97` (this
  record's sibling F1 measurement), plus DR-0025 E4's genuinely
  out-of-range control (`W=150u nf=1`) and its in-range twin (`W=90u nf=1`).
- **Operating conditions**: N/A -- single `.op` points, no load network.
- **Corner matrix run**: N/A -- simulator behaviour, not a PVT-conditioned
  design claim. Measured once per toolchain build below.

- **Result**: **F2 HOLDS on every ngspice build measured** -- every build
  produces exactly the same two outcomes DR-0025 E4/E5 found on ngspice-46
  (a declared bin, or a hard parse error), never a silent clamp:

  | build | `wnflag=0` | no card | out-of-range (`W=150u nf=1`, `wnflag=1`) | in-range control (`W=90u nf=1`, `wnflag=1`) | F2 |
  |---|---|---|---|---|---|
  | `ngspice-46-pinned` | `could not find a valid modelname` | same | same | resolves `pfet_03v3.12` | HOLDS |
  | `ngspice-46-fresh` | `could not find a valid modelname` | same | same | resolves `pfet_03v3.12` | HOLDS |
  | `ngspice-47` | `could not find a valid modelname` | same | same | resolves `pfet_03v3.12` | HOLDS |
  | `ngspice-42-apt` | `could not find a valid modelname` | same | same | resolves `pfet_03v3.12` | HOLDS |

  No build ever produced a third outcome (a resolved-but-wrong bin, a
  warning-only degraded run, or a value for the out-of-range control). DR-0025
  E4's "declared bin or hard error, nothing else" property is not
  ngspice-46-specific.

- **Links**:
  - Testbench: `sim/toolchain-portability/testbench/wnflag_facts.py`
    (`--facts F2`)
  - Raw per-build ngspice logs (four probes each --
    `f2-bin-resolve-wnflag0.log`, `f2-bin-resolve-no-card.log`,
    `f2-out-of-range-wnflag1.log`, `f2-in-range-control-wnflag1.log`):
    `sim/toolchain-portability/corners/20260919-034000-4595f97/ngspice-46-pinned/`,
    `.../ngspice-46-fresh/`, `.../ngspice-47/`, `.../ngspice-42-apt/`
  - Ratifies/extends: `spec/decision-records/DR-0025-model-bin-selection-binds-on-per-finger-width.md`
    (E4/E5, established on ngspice-46 only)
  - Amendment this record substantiates:
    `spec/decision-records/DR-0027-wnflag-facts-reproduce-across-ngspice-majors.md`
  - Sibling records (same build set, same host, same run): F1
    `20260919-033900-4595f97.md`, F3 `20260919-034100-4595f97.md`
  - Filed by: issue #254 (closed issue #221's un-performed scope)
- **Timestamp / author**: 2026-09-19T03:40:00Z, agent-builder (issue #254)
- **Supersedes**: nothing.

## Environment

Identical toolchain set, PDK, host and git commit as
`20260919-033900-4595f97.md` -- see that record's Environment section for
the full build provenance (`configure` flags, binary sha256s, apt package
version). Command run per build:
`python3 sim/toolchain-portability/testbench/wnflag_facts.py --ngspice
<build> --facts F2 --log-dir
sim/toolchain-portability/corners/20260919-034000-4595f97/<label>`

---

Append-only: never edit or delete this file -- a correction mints a new
record-id and points back here via **Supersedes**.

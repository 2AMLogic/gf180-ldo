# Record 20260919-033900-4595f97

- **Record ID**: 20260919-033900-4595f97
- **Claim**: issue #254's fact (a) / DR-0025's **F1** -- `.options wnflag=1`
  resolves `design/netlist/ldo_core.spice`'s pass-device geometry
  (`W=2000u nf=40`) to the declared bin `pfet_03v3.12`, with the instance's
  `w` left undivided at 2000 um -- reproduces on ngspice majors other than
  the 46 DR-0025 established it on. This is a statement about the
  **simulator**, not the design: no `sim/loop-stability`-style claim is made
  here, and none of DR-0025's own ratified evidence is edited or superseded
  (`sim/README.md`'s append-only rule; DR-0025's own "ratified records are
  never edited" convention).
- **Netlist provenance**: N/A -- this is a toolchain-portability measurement
  (see `sim/toolchain-portability/README.md`), not a DUT-netlist simulation.
  The probe deck instantiates the pass device's exact declared geometry
  (`W=2000u L=0.28u nf=40`, `pfet_03v3`) through the pinned PDK's own
  `.subckt` wrapper -- the identical construction DR-0025's own E2 evidence
  used -- rather than `.include`-ing `design/netlist/ldo_core.spice` itself.
- **Operating conditions**: N/A -- a single `.op` point, no load network, no
  enable sequencing. Bin selection depends only on device geometry and
  `wnflag`, not on bias or load (DR-0025 E2/E3).
- **Corner matrix run**: N/A -- this fact is simulator behaviour, not a
  PVT-conditioned design claim. Measured once per **toolchain build** below
  instead (four independently-provisioned ngspice binaries, one PDK, one
  host).

- **Result**: **F1 HOLDS on every ngspice build measured**, including
  ngspice-47, the scope target of issue #254:

  | build | banner | binary sha256 | resolved model | `w` | `nf` | `wnflag` readback | F1 |
  |---|---|---|---|---|---|---|---|
  | `ngspice-46-pinned` (this repo's pinned install) | `ngspice-46` | `c874869b16dc8a31cfa9d37d2b90a2a9e0e5b996f8e2535db89eb82855072931` | `pfet_03v3.12` | `0.002` | `40` | `1` | HOLDS |
  | `ngspice-46-fresh` (independently built, same `./configure` flags) | `ngspice-46` | `cec95020498753bee612522d9a8ca9a79aeaa19c01ca2e4d5f93db6695484ff0` | `pfet_03v3.12` | `0.002` | `40` | `1` | HOLDS |
  | `ngspice-47` (issue #254's scope target) | `ngspice-47` | `ad57436ca0adc819d62af9ded9cb910c1d733641c95d0f9d73ee8ca0d2600b9e` | `pfet_03v3.12` | `0.002` | `40` | `1` | HOLDS |
  | `ngspice-42-apt` (Ubuntu apt, also CI's `pvt-smoke` toolchain) | `ngspice-42` | `820658317b0b54035208da41936fd6871ce924036e5ea4113a4168b821b7fc45` | `pfet_03v3.12` | `0.002` | `40` | `1` | HOLDS |

  All four builds resolve the identical bin, with the identical undivided
  `w`, at the identical `nf`. Bin resolution under `wnflag=1` is not
  ngspice-46-specific.

- **Links**:
  - Testbench: `sim/toolchain-portability/testbench/wnflag_facts.py`
    (`--facts F1`)
  - Raw per-build ngspice logs (one probe each):
    `sim/toolchain-portability/corners/20260919-033900-4595f97/ngspice-46-pinned/f1-bin-resolve-wnflag1.log`,
    `.../ngspice-46-fresh/f1-bin-resolve-wnflag1.log`,
    `.../ngspice-47/f1-bin-resolve-wnflag1.log`,
    `.../ngspice-42-apt/f1-bin-resolve-wnflag1.log`
  - Ratifies/extends: `spec/decision-records/DR-0025-model-bin-selection-binds-on-per-finger-width.md`
    (E2, established on ngspice-46 only)
  - Amendment this record substantiates:
    `spec/decision-records/DR-0027-wnflag-facts-reproduce-across-ngspice-majors.md`
  - Filed by: issue #254 (closed issue #221's un-performed scope)
- **Timestamp / author**: 2026-09-19T03:39:00Z, agent-builder (issue #254)
- **Supersedes**: nothing -- this is the first `sim/` record of DR-0025's
  facts on any ngspice major other than 46.

## Environment

- Host: `ip-172-31-76-7` (x86_64, Linux 7.0.0-1010-aws)
- PDK: `gf180mcuD @ c6d73a35f524070e85faff4a6a9eef49553ebc2b` (the pinned
  hash, identical for every arm above)
- git: `4595f971066a9b255684c4dc88991bb5cb36a218` on `feature/issue-254`
- ngspice builds measured (banner / binary sha256 / provenance):
  - `ngspice-46-pinned` -- `c874869b16d...072931` -- this host's existing
    pinned install (`~/.local/bin/ngspice`; the same sha256 DR-0025's own
    filing-host evidence cites)
  - `ngspice-46-fresh` -- `cec9502049...484ff0` -- built from
    `ngspice-46.tar.gz` (ng-spice-rework 46) via
    `./configure --prefix=$HOME/.local/ngspice-46 --disable-debug
    --with-readline=yes --enable-openmp=no && make -j8 && make install`
  - `ngspice-47` -- `ad57436ca0a...2600b9e` -- built the same way from
    `ngspice-47.tar.gz` (ng-spice-rework 47), identical `configure` flags to
    `ngspice-46-fresh` per `docs/environment-setup.md` #1's "build the
    comparison arm the same way" guidance
  - `ngspice-42-apt` -- `820658317b0...b7fc45` -- Ubuntu 24.04 (`noble`)
    apt package `ngspice 42+ds-3build1`, the same toolchain CI's
    `pvt-smoke` job runs
- Command run per build:
  `python3 sim/toolchain-portability/testbench/wnflag_facts.py --ngspice
  <build> --facts F1 --log-dir
  sim/toolchain-portability/corners/20260919-033900-4595f97/<label>`

---

Append-only: never edit or delete this file -- a correction mints a new
record-id and points back here via **Supersedes**.

# Record 20260922-022122-ac57c94

- **Record ID**: 20260922-022122-ac57c94
- **Claim**: `README.md`'s **ratified** `Stability` row -- the narrowed
  envelope DR-0018 proposed and the operator ratified on 2026-09-15 by
  merging PR #199 (`909a060`): *stable 0.1-50 mA at `C_eff` = 1 uF nominal,
  ESR >= 200 mOhm; PM >= 45 deg, GM >= 10 dB worst corner, DR-0008
  resurgence clean* -- **re-verified against the design as it stands today**,
  rather than against the 2026-09-06 netlist every previously committed
  statement of that row was measured on. This is issue #51's Acceptance
  Criteria #2/#3/#4 discharged on current evidence: the full 4536-point
  matrix was re-run unmodified (head record
  `20260922-012628-ac57c94`, this record's companion), and this record is the
  subset accounting that head record does not contain on its own -- which
  points inside the ratified envelope pass, and which decision record backs
  every point outside it.
  **This record makes no new DR-0001 (full 0.33-4.7 uF / no-minimum-ESR
  window) claim**: that verdict is, and remains, **FAIL**, stated by the head
  record itself (2939/4536) and by DR-0018's own Decision #1, and nothing
  here alters it.
- **Netlist provenance**: **derived** -- no new simulation was run for this
  record. Its inputs are two already-committed matrix CSVs:
  - head `20260922-012628-ac57c94` (this branch; DUT
    `design/netlist/ldo_core.spice` at `ac57c94`, i.e. the post-#189/#191
    FB-injection soft-start with DR-0023's cascoded injection mirror (#250)
    and DR-0024's ramp-capacitor resize (#223)), and
  - baseline `20260906-071437-fff0bf0` (the record this head supersedes; DUT
    at `fff0bf0`, i.e. the pre-#189 clamp-comparator soft-start),

  joined point-by-point by `sim/loop-stability/testbench/compare_records.py`
  on `(corner_id, iload_ma, ceff_uf, esr_ohm)`. All 4536 keys match one-to-one
  with no unmatched points on either side.
- **Corner matrix run**: the full ratified matrix on both sides -- 63 PVT
  points x 6 load x 3 cap x 4 ESR = **4536 points per record**. The ratified
  `Stability` envelope is the 5 load x 1 cap x 2 ESR x 63 PVT = **630-point**
  subset of it (0.1/1/10/25/50 mA, `C_eff` = 1 uF, ESR in {0.2, 0.5} ohm).
  Per DR-0018 Consequence #2 the bench itself is **not** narrowed -- the full
  grid is still what is measured; only the claimed envelope is narrower, and
  restating the verified subset is this record's job.
- **Statistical convention**: N/A -- a corner-matrix claim plus a
  point-by-point comparison of two corner matrices, not a distribution claim.

- **Result**:

  - **The ratified envelope holds on the current design: 630/630, with
    margin to spare on both bars.**

    | | ratified bar | baseline `fff0bf0` (2026-09-06 DUT) | head `ac57c94` (today's DUT) |
    |---|---|---|---|
    | points passing, 0.1-50 mA / 1 uF / ESR >= 200 mOhm | 630/630 | 630/630 | **630/630** |
    | worst phase margin in the envelope | >= 45 deg | 55.44 deg | **55.75 deg** (`res_ss_-40c_3.63v`, 50 mA, 0.2 ohm) |
    | worst gain margin in the envelope | >= 10 dB | 11.96 dB | **12.08 dB** (`res_ss_-40c_2.97v`, 50 mA, 0.5 ohm) |
    | DR-0008 resurgence flagged in the envelope | 0 | 0/630 | **0/630** (worst -0.2214 dB, `res_ff_125c_3.63v`, 50 mA, 0.5 ohm) |

    Per load column, inside the envelope:

    | `I_load` | `C_eff` = 1 uF, ESR >= 200 mOhm |
    |---|---|
    | 0.1 mA | 126/126 |
    | 1 mA | 126/126 |
    | 10 mA | 126/126 |
    | 25 mA | 126/126 |
    | 50 mA | 126/126 |
    | **0.1-50 mA total** | **630/630 (100.0%)** |

    The worst points are the same two `res_ss`/-40 C/50 mA cells DR-0018
    identified on the superseded matrix, and both moved *away* from their
    bars, not toward them (+0.31 deg, +0.12 dB). That movement is not claimed
    as a design improvement -- see "What this record does not claim" below.

  - **Every point outside the ratified envelope is accounted for by a
    decision record, not left implied.** Counts re-derived from this head
    record's own CSV (the same grouping DR-0018 used on the superseded one):

    | slice | head `ac57c94` | backed by |
    |---|---|---|
    | 0 mA, all caps, all ESR | 1/756 | **DR-0007** (0 mA is outside the envelope; `gm_pass` collapses ~110x below 0.1 mA) |
    | 0.1-50 mA, 0.33 uF, ESR >= 200 mOhm | 353/630 | **DR-0018** Decision #1 (cap window narrowed to 1 uF nominal), with the structural cause in DR-0015/DR-0017 and the only remaining in-silicon lever measured foreclosed by **DR-0019** (issue #147) |
    | 0.1-50 mA, 4.7 uF, ESR >= 200 mOhm | 603/630 | same |
    | 0.1-50 mA, 1 uF, all ESR (>= 1 mOhm) | 1210/1260 | **DR-0018** Decision #1 (ESR floor of 200 mOhm) |
    | 0.1-50 mA, full cap window, ESR >= 200 mOhm | 1586/1890 | same |
    | **whole 4536-point matrix (DR-0001's original window)** | **2939/4536, FAIL** | the head record's own verdict; unchanged conclusion since DR-0015 |

    The single passing 0 mA point (`ss_-40c_3.63v` / 0.33 uF / 0.5 ohm, PM
    **45.00** deg) is the same boundary point DR-0018's own re-verification
    found at 45.01 deg on the superseded matrix -- a point sitting *on* the
    bar to two decimals, which
    `sim/loop-stability/records/20260906-090647-3981d88.md` already documents
    as this bench's boundary-point noise. It is not a reopening of DR-0007's
    0 mA conclusion, and this record does not treat 0 mA as passing anywhere.

  - **Nothing regressed anywhere in the full matrix across the soft-start
    redesign.** Point-by-point join, baseline `fff0bf0` -> head `ac57c94`:

    | | count |
    |---|---|
    | DR-0001 PASS, baseline | 2929/4536 |
    | DR-0001 PASS, head | 2939/4536 (**+10**) |
    | PASS -> FAIL | **0** |
    | FAIL -> PASS | 10 |
    | DR-0008 resurgence flagged, either side | 0/4536 |

    Per-point movement over the common 4536 points: median \|dPM\| = 0.080
    deg, p99 = 1.140 deg, max 1.930 deg (`res_ss_125c_2.97v` / 1 mA / 0.33 uF
    / 0.5 ohm); median \|dGM\| = 0.190 dB, p99 = 26.240 dB, max 35.790 dB
    (`res_ff_125c_2.97v` / 0 mA / 4.7 uF / 1 mOhm -- a 0 mA point whose GM is
    read against a ~85 dB-deep, near-flat high-frequency floor, where a tiny
    phase movement relocates `f_180` by decades; it is not an
    envelope point and not a margin that is near its bar); median
    \|d(crossover)\| = 0.045 %, p99 = 1.970 %, max 2.914 %. `resurgence_db`
    movement (max 0.099 dB) is **COMPARABLE** by the issue-#69 criterion --
    within `selftest.py`'s 1.75 dB `AC_DEC`-invariance tolerance -- and both
    records were minted at `dec 400` with the fixed-offset deck, so the
    column is a like-for-like read.

    All 10 flips are FAIL -> PASS and all 10 sit within 1.5 deg / 0.2 dB of
    their bar (largest: `sf_27c_3.63v` / 1 mA / 0.33 uF / 0.05 ohm, PM
    43.78 -> 45.22); all 10 sit outside the ratified envelope (each is either
    a 0.33 uF point or an ESR < 200 mOhm point), which is why the envelope
    count is 630/630 on both sides of the join rather than moving with them.
    See `20260922-022122-ac57c94-delta.csv`, rows where
    `result_base != result_head`.

  - **What this record does not claim.** It does **not** claim the
    soft-start redesign (#189/#191/#250) improved loop stability. The +10
    net flips and the +0.31 deg / +0.12 dB movement at the envelope's worst
    points are the same magnitude as this bench's already-documented
    cross-invocation drift at marginal points (issue #182, root-caused as a
    toolchain-identity gap in `c565098`; `20260906-072922-fff0bf0.md`
    reproduced ~2 deg of it on a netlist that had not changed at all), and
    the baseline record predates the binary-sha/host provenance fields that
    would let the two toolchains be compared directly. The claim that *is*
    supported, and the one issue #51 needs, is the **directional** one:
    across a soft-start topology replacement, a cascoded injection mirror and
    a 5x ramp-capacitor resize, **zero points moved PASS -> FAIL anywhere in
    the 4536-point matrix**, and the ratified envelope is still 100% clean
    with both worst-case margins clear of their bars by ~10.7 deg and
    ~2.1 dB.

- **Reproduction** (runs no simulation; both inputs are committed):

  ```
  # full-matrix join, verdict flips and movement statistics
  python3 sim/loop-stability/testbench/compare_records.py \
      20260906-071437-fff0bf0 20260922-012628-ac57c94 \
      --write-delta 20260922-022122-ac57c94

  # the ratified-envelope subset counts, from the head record's own CSV
  python3 - <<'PY'
  import csv
  rows = list(csv.DictReader(open(
      "sim/loop-stability/records/20260922-012628-ac57c94-matrix.csv")))
  env = [r for r in rows
         if abs(float(r["ceff_uf"]) - 1.0) < 1e-9
         and float(r["esr_ohm"]) >= 0.2
         and float(r["iload_ma"]) > 0]
  print(len(env), sum(r["result"] == "PASS" for r in env))
  print(min(float(r["phase_margin_deg"]) for r in env),
        min(float(r["gain_margin_db"]) for r in env),
        max(float(r["resurgence_db"]) for r in env))
  PY
  ```

- **Environment**: derived -- no simulator was invoked for this record. The
  measurement environment is the head record's
  (`sim/loop-stability/records/20260922-012628-ac57c94.md`): PDK gf180mcuD @
  `c6d73a35f524070e85faff4a6a9eef49553ebc2b`, ngspice-46 (binary sha256
  `4f076ec866c69842d3a9e93376ac17b3ae0e1709b4ddb05890e0e6488a900b10`), host
  `robb-studio`, git `ac57c94`, AC sweep `dec 400 0.01 1e9` per injection.
  `compare_records.py` and the snippet above were run on the same checkout
  with python 3.14.

- **Links**:
  - Head (superseding) full-matrix record:
    `sim/loop-stability/records/20260922-012628-ac57c94.md` (+
    `-matrix.csv`, + `sim/loop-stability/corners/20260922-012628-ac57c94/`, +
    `sim/loop-stability/netlist-snapshots/20260922-012628-ac57c94.spice`)
  - Superseded baseline: `sim/loop-stability/records/20260906-071437-fff0bf0.md`
    (+ `-matrix.csv`)
  - Per-point delta for the full 4536-point join, machine-readable:
    `sim/loop-stability/records/20260922-022122-ac57c94-delta.csv`
  - Ratified envelope: `spec/decision-records/DR-0018-narrow-stability-envelope-to-1uf-nominal.md`
    (ratified 2026-09-15 by merging PR #199, `909a060`), `README.md`
    `Stability` row
  - 0 mA exclusion: `spec/decision-records/DR-0007-light-load-stability-envelope.md`
  - Why no further circuit lever was attempted:
    `spec/decision-records/DR-0019-buffer-standing-current-blocked-by-amp-iq-allocation.md`
    (issue #147, `f_hi` blocked by the amplifier's own `iq_ua` allocation),
    on top of DR-0015/DR-0016/DR-0017
  - DR-0008 precondition (this record's resurgence column):
    `spec/decision-records/DR-0008-loop-gain-rhp-pole-precondition.md`
  - Boundary-point noise characterization:
    `sim/loop-stability/records/20260906-090647-3981d88.md`; cross-invocation
    drift root-cause: issue #182
  - Issue: #51

---

Derived record in the `sim/README.md` format. Append-only: never edit or
delete this file -- a re-run or correction mints a new record-id and points
back here via **Supersedes**.

Placeholder raw-output stub — sim/README.md worked example (issue #5).

This is NOT real ngspice output and does NOT represent a real simulation
result. It stands in for the raw per-corner log a real dropout-vs-load run
would produce at this corner (ss process, -40C, 2.97V supply, 50mA load —
the worst-case dropout corner), so the worked example's directory layout
is complete.

A real log here is the captured ngspice stdout/measurement output for this
exact corner point, produced by the #2 harness (or a standalone deck per
#4's interim fallback) and referenced from the corresponding
records/20260730-120000-4e9c1a2.md summary record.

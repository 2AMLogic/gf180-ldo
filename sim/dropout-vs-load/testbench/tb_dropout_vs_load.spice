* Placeholder testbench stub — sim/README.md worked example (issue #5)
*
* This is NOT a runnable ngspice deck and does NOT represent a real
* simulation. It exists only to populate the testbench/ directory the
* sim/README.md convention calls for, so the worked example's directory
* layout matches what a real dropout-vs-load record will look like once
* the #2 harness and #3/#8 schematic land.
*
* When #2/#8 land, replace this stub with the real dropout-vs-load
* testbench netlist fragment (DUT instantiation, load-current source,
* output cap + ESR network, measurement statements) and re-derive a real
* record — do not edit this placeholder in place; the real record
* supersedes nothing here since no real record has ever existed under
* this slug.

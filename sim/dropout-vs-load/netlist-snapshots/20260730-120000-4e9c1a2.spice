* Placeholder netlist snapshot — sim/README.md worked example (issue #5)
*
* This is NOT a real DUT netlist and does NOT represent a real simulation.
* It stands in for the frozen netlist snapshot that record
* 20260730-120000-4e9c1a2.md (records/) would reference, so the worked
* example's directory layout is complete and unambiguous.
*
* A real snapshot is a frozen copy of design/ldo.sch's netlist export as
* of the git sha in the record ID, taken at record-creation time so later
* edits to design/ don't retroactively change what a past record proves.

# Record 20260816-080440-8e105a0

- **Record ID**: 20260816-080440-8e105a0
- **Supersedes**: `20260802-105338-c828e73` (stale as of this run — netlist
  sha256 predated PR #78 / DR-0015, the `Mrza`/`Rza` adaptive-compensation
  shelf landed in `design/error_amp.sch`)
- **Claim**: `README.md` target-spec **"Current limit"** row — *65–80 mA over
  PVT, constant-current (brickwall) clamp; never engages for `I_load` ≤ 50 mA
  at any corner; survives a continuous Vout = 0 short at Vin_max (note 5)* —
  plus the **Thermal** row's *≤ 290 mW into a Vout = 0 short*. Same four
  sub-claims, same verdict structure, as the superseded record.
- **Why this re-run exists**: issue #100 — the block's T1/bronze re-read
  (#95) found the superseded record's DUT netlist sha256 no longer matched
  current `design/netlist/ldo_core.spice`: PR #78 (`1970aea`, 2026-08-08,
  DR-0015) landed the `Mrza`/`Rza` adaptive-compensation shelf in
  `design/error_amp.sch` after that record was taken. This run re-measures
  against the current netlist sha and re-states pass/fail against the
  **ratified** window (65–80 mA) and thermal bound (≤ 290 mW) — it does not
  relax or reinterpret either bound, and it does not touch the unratified
  `DR-0005` proposal (see References).
- **Netlist provenance**: schematic — `design/netlist/ldo_core.spice`,
  frozen at `netlist-snapshots/20260816-080440-8e105a0.spice` — taken
  against a **clean working tree** at `8e105a0` (`main`, post-#78/DR-0015).
  `python3 design/netlist.py --check` confirms the netlist reproduces the
  committed schematic byte-for-byte.
  - **DUT netlist sha256**:
    `3aeb225910549530c8e12875c8065cf645d1ae28e4754e3e638d9a13d067813e` —
    matches `design/netlist/ldo_core.spice` at the time of this run (verified
    via `shasum -a 256`), and matches the frozen snapshot above.
- **Corner matrix run**: 63 points — process {tt, ff, ss, fs, sf, res_ff,
  res_ss} × T {−40, 27, 125 °C} × Vin {2.97, 3.30, 3.63 V}. Full factorial,
  all completed (no subset). `bjt_ff`/`bjt_ss` omitted (no bipolar device in
  this design, per this testbench's long-standing convention).
- **Operating conditions**: unchanged from the superseded record —
  regulation mode (`Rload` = ∞ / 72 Ω / 36 Ω) and V-I mode (`Vout` forced to
  1.764, 1.5, 0.9, 0.3, 0.0 V), no output cap (DC claim), enabled throughout,
  load current measured rather than assumed.
- **Statistical convention**: N/A — corner-matrix claim.

## Result: verdict unchanged — FAIL against both ratified bounds, same corner classification as the stale record

| Quantity | superseded record (`c828e73`) | this record (`8e105a0`) |
|---|---|---|
| onset at `Vout` = 1.764 V | 62.01 … 93.83 mA | **62.01 … 93.83 mA** |
| onset into a dead short | 62.59 … 95.10 mA | **62.59 … 95.10 mA** |
| flatness, 0 V vs 1.5 V | +0.32 … +2.30 % | **+0.32 … +2.30 %** |
| `Vout` at a 50 mA resistive load | 1.7989 … 1.7995 V | **1.7988 … 1.7995 V** |
| sense-domain margin at 50 mA | ≥ 18.75 % | **≥ 18.75 %** |
| dissipation into a short, worst corner | 345.2 mW @ `ff_125c_3.63v` | **345.2 mW @ `ff_125c_3.63v`** |
| corners outside the ratified 65–80 mA window | 36 / 63 | **36 / 63** |
| corners exceeding the ratified ≤ 290 mW thermal bound | not separately tallied in superseded record | **12 / 63** |
| corners where the limit engages at or below 50 mA | none | **none** |
| corners outside the ±2 % window at 50 mA load | none | **none** |

**Corners changed classification (in ↔ out of the 65–80 mA window) relative
to the superseded record: zero.** A point-by-point diff of this run's
`summary.csv` against `c828e73`'s shows every measured column bit-identical
to within floating-point noise (one corner, `sf_-40c_3.63v`, differs by
0.0001 mA in `ilim_1500_ma` — below the significant digits either record
reports on). This is expected and confirms the superseded record's own
prediction: the current-limit / thermal testbench is a DC / quasi-static V-I
sweep, and PR #78's `Mrza`/`Rza` addition is a Miller-network change in the
error amplifier's AC compensation path — it does not touch the DC
current-limit / brickwall-clamp arbitration node (`design/ldo_ilimit.sch`'s
`CLG` / `Mclamp_bg`) this testbench measures.

- **Overall (current limit, ratified 65–80 mA window): FAIL** — 36/63
  corners outside the window (measured 62.0–93.8 mA), same as the superseded
  record. Assessed against the ratified window only; `DR-0005`'s proposed
  relaxed window is unratified (see References) and is not used here.
- **Overall (thermal, ratified ≤ 290 mW into a short): FAIL** — worst-case
  345.2 mW at `ff_125c_3.63v`, 12/63 corners exceed 290 mW, all at
  `Vin` ≥ 3.30 V in the `ff` / `res_ff` fast-nmos-resistor corner family.
- **Overall (never engages ≤ 50 mA / brickwall flatness / ±2 % window at
  50 mA): PASS** — unchanged from the superseded record.

Full per-corner values: `sim/current-limit/corners/20260816-080440-8e105a0/summary.csv`.

## Environment

- PDK: gf180mcuD @ c6d73a35f524070e85faff4a6a9eef49553ebc2b
- ngspice: ngspice-47
- git: `8e105a0` on `main` (clean working tree)
- Command: `./sim/current-limit/testbench/run.sh`
- Raw logs: `sim/current-limit/corners/20260816-080440-8e105a0/`
- Rollup: `sim/current-limit/corners/20260816-080440-8e105a0/summary.csv`
- Wall time: 329.49 s (5:29, 63 points, `-P 4`)

## References

- Superseded record: `sim/current-limit/records/20260802-105338-c828e73.md`
- Staleness root cause: PR #78 / DR-0015 (`design/error_amp.sch`
  `Mrza`/`Rza` adaptive-compensation shelf)
- Verdict source for the staleness finding: #95's closing comment (T1/bronze
  re-read, item 5)
- Spec-row dispute (separate, unratified, out of scope for this record):
  `spec/decision-records/DR-0005-current-limit-window.md`
- Gap-to-T1 tracker: #79

---

Append-only: never edit or delete this file — a re-run or correction mints a
new record-id and points back here via **Supersedes**.

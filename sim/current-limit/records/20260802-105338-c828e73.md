# Record 20260802-105338-c828e73

- **Record ID**: 20260802-105338-c828e73
- **Supersedes**: `20260802-023438-5ea33e8` (provenance-corrected by
  `20260802-085342-8a1a9e8`; compared against that record, not the
  correction, since the correction changes no measured value)
- **Claim**: `README.md` target-spec **"Current limit"** row — *65–80 mA over
  PVT, constant-current (brickwall) clamp; never engages for `I_load` ≤ 50 mA
  at any corner; survives a continuous Vout = 0 short at Vin_max (note 5)* —
  plus the **Thermal** row's *≤ 290 mW into a Vout = 0 short*. Same four
  sub-claims, same verdict structure, as the superseded record.
- **Why this re-run exists**: issue #53 added `Cf1`/`Cf2` (a Miller cap
  `N1` → `BG` around the stage-2 driver `M2P`) to `design/error_amp.sch`,
  fixing the local `Rz`/`Cc` loop's own RHP-pole instability
  (`spec/decision-records/DR-0008-loop-gain-rhp-pole-precondition.md`). This
  is a spot-check that the `BG`-arbitration contract `design/ldo_ilimit.sch`'s
  "CLAMP AUTHORITY AGAINST THE REAL AMPLIFIER" note requires re-running for
  any change to the pass-gate output stage still holds, since `sim/enable-
  shutdown/records/20260802-095233-c828e73.md`'s large-signal transient
  measured a real (if modest) shift concentrated at cold corners.
- **Netlist provenance**: schematic — `design/netlist/ldo_core.spice`, frozen
  at `netlist-snapshots/20260802-105338-c828e73.spice` — **taken against a
  dirty working tree**; not citable as a clean-tree result, though
  `python3 design/netlist.py --check` confirms the netlists reproduce the
  committed schematics byte-for-byte.
- **Corner matrix run**: 63 points — process {tt, ff, ss, fs, sf, res_ff,
  res_ss} × T {−40, 27, 125 °C} × Vin {2.97, 3.30, 3.63 V}. Full factorial,
  all completed. `bjt_ff`/`bjt_ss` omitted (no bipolar device).
- **Operating conditions**: unchanged — regulation mode (`Rload` = ∞ / 72 Ω /
  36 Ω) and V-I mode (`Vout` forced to 1.764, 1.5, 0.9, 0.3, 0.0 V), no
  output cap (DC claim), enabled throughout, load current measured rather
  than assumed.
- **Statistical convention**: N/A — corner-matrix claim.

## Result: every sub-claim keeps its previous verdict, and every number is unmoved

| Quantity | superseded record | this record |
|---|---|---|
| onset at `Vout` = 1.764 V | 62.01 … 93.83 mA | **62.01 … 93.83 mA** |
| onset into a dead short | 62.59 … 95.10 mA | **62.59 … 95.10 mA** |
| flatness, 0 V vs 1.5 V | +0.32 … +2.30 % | **+0.32 … +2.30 %** |
| `Vout` at a 50 mA resistive load | 1.7989 … 1.7995 V | **1.7989 … 1.7995 V** |
| sense-domain margin at 50 mA | ≥ 18.75 % | **≥ 18.75 %** |
| dissipation into a short, worst corner | 345.2 mW | **345.2 mW** |
| corners outside the ratified 65–80 mA | 36 / 63 | **36 / 63** |
| corners where the limit engages at or below 50 mA | none | **none** |

This is a DC / quasi-static V-I sweep, not sensitive to `Cf1`/`Cf2`'s small
Miller cap (an AC-domain fix in a completely different part of the frequency
axis) or to the large-signal transient shift `sim/enable-shutdown/` measured
— confirmed here: every column is bit-identical to the superseded record, no
value moves outside floating-point noise. `Mclamp_bg` is unaffected because
it sits on `CLG`, a node this issue's change does not touch.

## Environment

- PDK: gf180mcuD @ c6d73a35f524070e85faff4a6a9eef49553ebc2b
- ngspice: ngspice-46
- git: `c828e73` on `feature/issue-53` (dirty at generation time — see
  Netlist provenance)
- Command: `./sim/current-limit/testbench/run.sh`
- Raw logs: `sim/current-limit/corners/20260802-105338-c828e73/`
- Rollup: `sim/current-limit/corners/20260802-105338-c828e73/summary.csv`

---

Append-only: never edit or delete this file — a re-run or correction mints a
new record-id and points back here via **Supersedes**.

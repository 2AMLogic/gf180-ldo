# Record 20260731-232504-f1b260b

- **Record ID**: 20260731-232504-f1b260b
- **Claim**: Loop-closure sanity for `design/ldo_core.sch` (issue #8) -- NOT a
  ratified spec-line measurement. Confirms the core regulates near its 1.8 V
  target with the placeholder error amp, that the enable stub gates the
  circuit into a low-current state, and that no node is rail-stuck, per
  issue #8's acceptance bar ("DC operating-point sanity sim at nominal
  conditions"). A future ratified-spec claim (e.g. output-accuracy,
  `spec/ldo.md#output-voltage` once such an anchor exists) would supersede
  this record's role as evidence for that number; this record's own claim is
  narrower: the loop closes, the amp placeholder's polarity/gain choice
  behaves as designed, and the enable stub is functional.
- **Netlist provenance**: schematic (`design/ldo_core.sch`, via
  `design/netlist/ldo_core.spice`)
- **Corner matrix run**:
  - Process: tt (typical) only
  - Temperature: 27 degC only
  - Supply: 3.30 V (nominal) only
  - 1 PVT point (not the full CLAUDE.md-mandated -40/27/125 degC x process x
    +/-10% supply matrix).
  - **Subset reason**: issue #8's acceptance criterion is explicitly a
    single-point "DC operating-point sanity sim at nominal conditions", not a
    corner-swept spec claim -- the curator enhancement on #8 explicitly
    scopes this issue to nominal-only and defers full PVT sweeps to the
    spec-line testbenches under future issues (e.g. #10 stability, #12
    dropout-vs-load). A full-matrix rerun of this same loop-closure check
    would be redundant with those, not additive.
- **Operating conditions**:
  - Load current: 1.0 mA (`Rload` = 1.8 kOhm from VOUT to ground; a light,
    realistic load -- not a spec load point, just enough to confirm the loop
    regulates under nonzero draw, not only into the feedback divider's own
    ~6 uA bias)
  - Output cap: 1.0 uF + 100 mOhm ESR (testbench-side component per issue
    #8's compensation guidance; inert at DC, included for schematic realism)
  - Enable state: BOTH enable states run under this one record-id (two
    corner logs, one for each): `tt_27c_3.30v` is EN=VIN=3.3 V (enabled,
    the claim above); `tt_27c_3.30v_endis` is EN=0 V (disabled), a
    supplementary confirmation for the test-plan item "confirm the enable
    stub gates the circuit into a low-current state" -- not a second
    independent claim.
- **Statistical convention**: N/A (single-point sanity claim, not a
  distribution claim)
- **Result**:

  | corner-id | EN (V) | Vout (V) | loop/PASS_GATE (V) | FB (V) | VREF (V) | I(VIN) (mA) |
  |---|---|---|---|---|---|---|
  | `tt_27c_3.30v` | 3.3 (enabled) | 1.802416 | 2.546657 | 0.600805 | 0.6 | 1.00735 |
  | `tt_27c_3.30v_endis` | 0 (disabled) | 2.47e-7 | 3.299708 | 8.25e-8 | 0.6 | 0.0032999 |

  Verdict against issue #8's Test Plan:
  - Vout near 1.8 V target, enabled: **PASS** (1.8024 V, 0.13% high -- the
    expected finite-gain error of a 3162x/~70 dB placeholder amp closing a
    divide-by-3 feedback loop, not a bug)
  - No rail-stuck nodes, enabled: **PASS** (Vout, FB, and the loop node all
    sit at sane intermediate values, none pinned at VIN or VSS)
  - Enable stub gates a low-current state: **PASS** (Vout collapses to
    ~0 V, the pass-device gate is pulled to ~VIN by the enable clamp
    turning the pass FET off, and supply current drops ~300x, from
    1.007 mA to 3.3 uA)
  - Loop-break point (ERRAMP_OUT / PASS_GATE) reachable from a testbench
    without modifying the core: **PASS** (this testbench ties the two
    ports together at instantiation -- `Xdut VIN VOUT EN 0 LOOP LOOP
    ldo_core` -- entirely at the testbench level; `design/ldo_core.sch` is
    untouched)
  - **Overall: PASS**
- **Links**:
  - Testbench: `sim/op-point-sanity/testbench/tb_op_point_sanity.spice.in`,
    `sim/op-point-sanity/testbench/run.sh`
  - Netlist snapshot: `sim/op-point-sanity/netlist-snapshots/20260731-232504-f1b260b.spice`
  - Raw logs: `sim/op-point-sanity/corners/20260731-232504-f1b260b/`
- **Timestamp / author**: 2026-07-31T23:25:04Z, agent-builder (issue #8)
- **Supersedes**: (none -- first record for this experiment)

## Environment

Everything needed to re-run this record:

- PDK: gf180mcuD @ open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b`
  (found via `sim/harness/pdk.py`)
- ngspice: ngspice-46
- git: `f1b260b` on `feature/issue-8` (clean at generation time)
- Command: `./sim/op-point-sanity/testbench/run.sh`

---

Written by `sim/op-point-sanity/testbench/run.sh` (hand-formatted record, per
`sim/README.md` -- this experiment predates any repo-wide automation of the
record format). Append-only: never edit or delete this file -- a re-run or
correction mints a new record-id and points back here via **Supersedes**.
